/**
 * 字典类型
 * @export
 * @interface dictModel
 */
export interface dictModel {
	/**
	 * 名称
	 * @type {string}
	 * @memberof dictModel
	 */
	name: string;
	/**
	 * 唯一标识符
	 * @type {number}
	 * @memberof dictModel
	 */
	id: number | string;
}
